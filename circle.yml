machine:
  python:
    version: 3.6.0

dependencies:
  post:
    - pip install -r dev-requirements.txt

test:
  override:
    - nosetests --with-coverage --cover-inclusive --cover-package=phial

  post:
    - flake8 phial/ tests/
    - coveralls
